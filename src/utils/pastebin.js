const request = require('electron').remote.require('request');
const CONFIG = require('./config.js');

export function shareCrew(roster, callback) {

	// TODO: find a website where the user could share their crew (for example to gather feedback on forums / reddit, or just to brag)
	alert('Not yet implemented!');
	// https://pste.eu/
	// https://notehub.org/
	// https://www.bitballoon.com/

	//callback(url);
}